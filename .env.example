# Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control - .env is already in .gitignore

# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=postgres_db
POSTGRES_PORT=5433

# JWT Configuration (IMPORTANT: Key must be at least 32 characters)
JWT_KEY=your-secret-key-minimum-32-characters-for-hs256-algorithm-security
JWT_ISSUER=postgres-minimal-api
JWT_AUDIENCE=postgres-minimal-api-clients
JWT_EXPIRATION_HOURS=1

# Database Connection String
# Will be automatically built using the variables above, or you can specify directly:
# ConnectionStrings__DefaultConnection=Host=localhost;Port=5433;Database=postgres_db;Username=postgres;Password=your_password

# CORS Configuration (comma-separated list)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:3005,http://localhost:8080

# Environment
ASPNETCORE_ENVIRONMENT=Development

# Note: To use these environment variables in the application,
# they need to be loaded via DotNetEnv or set as system environment variables
