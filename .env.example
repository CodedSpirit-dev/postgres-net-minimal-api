# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=postgres_db
POSTGRES_PORT=5433

# Application Configuration
JWT_KEY=your_jwt_secret_key_at_least_32_characters_long
JWT_ISSUER=tu_api_minimal
JWT_AUDIENCE=clientes_de_tu_api

# Connection String will be automatically built using the variables above
# Or you can use: ConnectionStrings__DefaultConnection="Host=localhost;Port=5433;Database=postgres_db;Username=postgres;Password=your_password"
